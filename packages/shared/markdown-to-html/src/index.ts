/**
 * @beautinique/shared-markdown-to-html
 *
 * Renders a Markdown file (typically a service's own `README.md`) into a
 * single, self-contained, GitHub-styled HTML page at build time - see
 * `generateHtmlFromMarkdown` for usage.
 */
export * from './generate-html-from-markdown.js';
export type * from './types/index.js';

